As the description says, this code exists to generate a cobweb diagram. It generates
within the bounds of [0,1], although it can be hard-coded to be changed.
2 Python libraries are used: matplotlib and numpy, both of which are required for python
graphing.
It was created on Python version 3.8